# ── Core (required) ──
requests>=2.28
trafilatura>=1.6
beautifulsoup4>=4.12
python-dateutil>=2.8
urllib3>=1.26
lxml>=4.9

# ── Optional extras (install via pip extras or ./install.sh) ──
# SPA / JS-rendered + bot-protection page support:
#   pip install "tofu-search[playwright]"  &&  python -m playwright install chromium
# PDF extraction:
#   pip install "tofu-search[pdf]"          # pymupdf>=1.23, pymupdf4llm>=0.0.5
# Everything:
#   pip install "tofu-search[all]"
